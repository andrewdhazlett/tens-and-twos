{
  "presets": [
    [
      "@babel/preset-env",
      {
        "targets": "defaults, not ie <= 11, not edge > 0, not IE_Mob 11",
        "modules": false
      }
    ],
    "next/babel",
    "@babel/preset-react",
    "@babel/preset-typescript"
  ]
}
